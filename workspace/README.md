# Workspace

Put local working files here when running the subtitle workflow.

Suggested names:

- `media.mp4`
- `media.srt`
- `media.rule-cleaned.srt`
- `media_subtitled.mp4`

This folder is for private/generated files. The repository ignores media,
audio, and subtitle outputs by default.
